from .remindme import setup

__red_end_user_data_statement__ = (
    "This cog stores reminder timers (user id, channel/guild ids, end time, delivery preference, optional name), "
    "per-user presets (name to duration), and per-guild published presets (name to duration, optional owner). "
    "Timer and preset data is used to deliver reminders and to resolve preset names when creating reminders."
)
