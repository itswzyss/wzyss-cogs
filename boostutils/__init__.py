from .boostutils import setup

__red_end_user_data_statement__ = (
    "This cog stores per-guild configuration for tracked roles and boost notifications, "
    "plus per-guild mappings of user IDs to role IDs for tracked roles removed after "
    "booster loss so they can be restored when boosting resumes."
)
