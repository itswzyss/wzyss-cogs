from .lfg import setup as setup

__red_end_user_data_statement__ = (
    "This cog stores per-guild game lists, per-user availability and DM notify opt-in, "
    "and game requests (user ID, game name, timestamp) until approved or denied. "
    "Users can clear their own availability and opt out of DM notifications."
)
