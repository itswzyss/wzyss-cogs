from .giveaway import setup

__red_end_user_data_statement__ = (
    "This cog stores giveaway data including host, prize, end time, entries (user IDs), "
    "and optional claim state on a per-guild basis. Draft state for interactive setup is "
    "stored until the giveaway is launched or the draft is cancelled."
)
